#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;

    // perimeter calculate
    int perimeter = a + b + c;

    cout << perimeter << endl;

    return 0;
}